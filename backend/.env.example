PORT=5000
MONGODB_URI=mongodb://localhost:27017/advising_system
# Paste the Firebase service account JSON as a single line, or point to a file path instead.
FIREBASE_SERVICE_ACCOUNT_PATH=./src/config/firebase-service-account.json
CLIENT_ORIGIN=http://localhost:3000
